import { createStyles } from "@material-ui/styles";

const styles = () =>
  createStyles({
    inputField: {
      color: "white",
      background: "#282c34",
      height: "100vh",
      width: "50vw",
      fontSize: 16,
    },
  });

export default styles;
